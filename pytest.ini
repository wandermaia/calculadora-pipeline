[pytest]
markers =
    unit: testes unitários sem dependências externas
    integration: testes que precisam de infraestrutura (banco, fila, etc.)

[coverage:run]
source = src.calculadora
omit = tests/*
